<h1>Hello <%= @user.email %> from <%= @user.company_name %></h1>
<h4>Your Bulk Order for <%=@bulk_order.item.item_name%> has been completed!</h4>
<br><br>
<h3>Order details</h3>
<h5>Total Quantity: <%=@bulk_order.max_amount%> grams</h5>
<h5>Total Price: $<%=@item.price * @item.bulk_order_amount%></h5>
<br>
<h3>Buy Details</h3>
<ol>
<%@bulk_order.user_orders.each do |user_order|%>
<li><%=User.find_by_id(user_order.user_id).email%> bought <%=user_order.quantity%> grams for  $<%=user_order.total_price%></li>
<%end%>
<ol>

<span>  There were a total of <%=@bulk_order.users.count%> users who were a part of this order.</span>
